# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
go.sum

# temporary
**/tmp*/*

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
cmd/keystores/keystores
cmd/addresscheck/addresscheck
cmd/clientsetup/clientsetup
cmd/accountbalances/accountbalances
cmd/newwallets/newwallets
cmd/queryblocks/queryblocks
